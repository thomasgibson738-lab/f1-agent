# Backend (Render) dependencies — the FastAPI API server.
# The frontend (Netlify) is static and needs no build deps.
fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.3
requests>=2.32
requests-cache>=1.3
feedparser>=6.0
pyarrow>=15
